# .github

Default community health files for all my repositories.

GitHub falls back to this repo for any file a repository does not define itself,
so `FUNDING.yml` here puts the Sponsor button on every repo at once. A repo that
needs something different just adds its own copy.
